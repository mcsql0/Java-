server:
  port: 80


spring:
  application:
    name: consul-consumer-order
  cloud:
    consul:
      host: 103.152.171.252
      port: 8500
      discovery:
        service-name: ${spring.application.name}
        heartbeat:
          enabled: true

